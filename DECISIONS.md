# Engineering decisions

[PRODUCT](docs/PRODUCT.md) owns product scope. This ledger records decisions affecting architecture, security or money, newest first and one row per decision. Financial figures behind D-0063 and D-0071 stay in the private working file named under Not in git.

## Ledger

| id | date | decision | supersedes |
|---|---|---|---|
| D-0226 | 2026-09-18 | The username, URL and notes the entry pane draws move off `EntryNameSanitizer` onto `DisplayTextSanitizer`, which keeps every printable character and line breaks and replaces only what can make text misrepresent itself. The name rule also strips structural characters, right for a string that addresses an entry and wrong for one a person reads: it turned `https://example.test/path` into `https: example.test path`. Titles and group paths keep the name rule. A secret's own characters are never sanitized | the name rule on the three display fields (ui-review) |
| D-0225 | 2026-09-18 | A pasted secret loses one trailing line break and is otherwise refused whole if it holds anything a keyboard cannot type, rather than stripped: a silently altered value is stored as the secret with nothing on screen showing it, while a refusal is visible while the form is open. The rule is `Keypaste.Core.SecretInput`, so the Avalonia clipboard and the test double cannot hold two versions of it. `Ctrl/Cmd+V` is per field and the master-password fields do not set it | SECURITY.md's "paste is unsupported in all three fields" |
| D-0224 | 2026-09-18 | `verify.sh` runs only the profiles the working tree's changes map to, resumes with `--from`, and on Windows runs the offline fixtures in a pinned Linux container. A full run took 17 minutes here, 12 of them fixtures (`verify-release-matrix.sh`: 522 s in Git Bash, 40 s on Linux), and a step ran it per commit. An unmapped path runs everything and hosted CI stays the full gate, so a wrong map fails late, not never. Git Bash runs the fixtures only under `VERIFY_SCRIPTS_NATIVE=1` | D-0130's full run after every final edit |
| D-0223 | 2026-09-18 | The desktop's file pickers move behind `IVaultFilePicker`, answering a path or null, instead of the view calling `TopLevel.StorageProvider`. V-4.8 requires that a cancelled picker writes nothing, and with the picker in the view a cancel never reached the view model: the only assertion available was that nothing happened after calling nothing, which passes against code that writes before opening a dialog. Browse moved behind it too rather than leaving two ways to choose a file | the picker in `UnlockView.axaml.cs` |
| D-0222 | 2026-09-18 | The rules deciding whether a vault may be created — occupied path, empty password, unmatched confirmation, then the directory and the save — live in `Keypaste.Core` as `VaultCreation`, which `keypaste init` and the desktop both call (PRODUCT §4.2). The overwrite refusal is a safety rule: the file there is an encrypted vault nothing can read back, so two implementations are two chances to destroy one. `Vault.Create` stays permissive, as its fixtures require; `--selftest` moved onto it so app code can be forbidden `Vault.Create(` outright, keeping D-0050's gate argument true | the refusals in `InitCommand` |
| D-0221 | 2026-09-17 | Signing and publication move to the end of Working proposition, after the desktop work they serve, and 3.6a folds into 4.7c2 as its **Input** rather than its own row: the enrolment gates nothing else, and 3.5b, 5.5b and H.8 already carry external credentials that way. R.1 stays last as the milestone gate. An installer nobody can yet use is not worth signing | 3.6a as a separate row; the signing section's position ahead of the desktop work |
| D-0220 | 2026-09-17 | 3.2 and 3.3, the community introduction and its feedback, are withdrawn with H-0006 and H-0007. Nothing Needed them and no gate named them; PRODUCT requires a demo per slice (§5.1) and that announcements follow their release (§5.3), not that one is made. Their substance has owners: security reporting in 2.4a/2.4b, defect intake in CLAUDE.md. A later announcement is a new row | D-0176's introduction timing; D-0196; D-0131 |
| D-0219 | 2026-09-17 | 4.7c splits: 4.7c1 is the publication path, closed on its fakes evidence; 4.7c2 is the public signed release, shipping only after an identity to sign with and after 4.8, 4.9, E.1, 4.4 and V.2b for a desktop worth installing. A published installer that cannot create a vault is not a release, and a published path is immutable, so the version is spent either way. R.1 Needs 4.7c2; PRODUCT is unchanged | 4.7c's single row and R.1's 4.7c Need |
| D-0218 | 2026-09-17 | A stranger verifying a published MSI checks it against `release.yml`, which re-attested the bytes, not `app.yml`, which compiled and signed them; the candidate check binding the two is invisible at the origin, so the installer's origin claim is weaker than the CLI's. Closing it means `app.yml` publishing its own Sigstore bundle, larger than 4.7c and against D-0138's point that only a published bundle verifies without an account. D-0215's two-manifest shape leaves the seam open | none |
| D-0217 | 2026-09-17 | A package is offered for publication by `internal: false`, and that flag alone decides it. `require-release-assets.sh` refuses an offered package that is unsigned or whose policy is `none`; `--publishable` exits 2 for the one recorded not-yet state: policy `none`, every package internal and unsigned, `signing.until` naming what lifts it. So `app` takes its origin now and reaches no bucket until 3.6b flips the flags, turning the desktop leg on by definition rather than by memory | none |
| D-0216 | 2026-09-17 | `release.yml` re-attests the desktop bytes it publishes, so a component names `provenance.workflow` apart from the `workflow` that builds it: `verify-provenance.sh` checks the published app manifest against release.yml, not app.yml. The release bundle attests publication; app.yml's attests who compiled the bytes, and `verify-desktop-candidate.sh` checks that against the tag before staging. Packages come from the app run `require-green-gates.sh --run-id` resolves, so nothing is rebuilt | none |
| D-0215 | 2026-09-17 | The desktop publishes into the CLI's `v{version}/` prefix with a manifest and bundle of its own, not a second prefix or a merged manifest: one destination check, one upload, one immutable version, and the nine published manifests keep their shape. `require-release-assets.sh`, `release-completion.sh` and `verify-provenance.sh` take `--component`, defaulting to `cli`; the allowlist is the union published, the counts stay per component, and `published[]` is keyed on version and component | D-0138's single per-release manifest |
| D-0207 | 2026-09-17 | The WixToolset.Sdk digest moves out of app.yml into the definition as the msi package's `tool_sha512`, and `build-windows-installer.sh` owns the restore, the pin check and the build the workflow used to inline, as `build-linux-appimage.sh` already does for the AppImage. The matrix gate refuses an msi package with no SHA-512 and a workflow or script that restates the pin, so 4.7d's second builder cannot carry a second copy of it | D-0139's app.yml SHA-512 pin |
| D-0214 | 2026-09-17 | keypaste's MSI schedules `MajorUpgrade` `afterInstallFinalize`, so the previous version is removed only once the new install commits and a failure inside the transaction leaves it installed and runnable (F.14). WiX's default, `afterInstallValidate`, removed it before a file was copied, and `afterInstallExecute` would still not cover a failure in InstallFinalize, which is where run 35278766000's failure landed. The cost is disk: both versions' files exist until the install commits | Package.wxs's unscheduled MajorUpgrade |
| D-0213 | 2026-09-17 | 4.7d's second reading, run 35278766000, finds the interruption and a defect: replacing a candidate's cabinet stream fails the upgrade inside the transaction, at `InstallFinalize. Return value 3`, and F.14 records what followed, nothing installed and the user's files untouched, because `RemoveExistingProducts` had already committed. `kill-owner` is refuted with it: the log's `MSI (s) (7C:48)` prefix is no operating-system process, so ending what it resolves to leaves the install running. The probe reads a failed transaction and what came back, not rollback ops | D-0211's rollback-in-the-log reading |
| D-0212 | 2026-09-17 | 4.7d's first interruption reading, run 35271224803, finds none of three conclusive and says why each missed: an icacls deny was applied and the install still finished, the installer writing with SYSTEM privileges; truncating the package failed with 1603 only after InstallFiles and InstallFinalize returned 1, at Error 1316 re-caching it, leaving nothing installed and no rollback; and the `/V` process ended was not the one owning the transaction, whose server finished the install. `DisableRollback` is 0 in every log | none |
| D-0211 | 2026-09-17 | 4.7d keeps its interruption as a question it measures: `interrupt-probe.yml` and `probe-msi-interruption.sh` are its diagnosis tooling, not a step, and record a reading per method rather than a verdict, a method counting only when the log names a rollback and the lower version is registered and runnable. No conclusive method is a result that names the next experiment (docs/diagnostics.md). The candidate build moves into `build-upgrade-candidates.sh`, shared with the probe, so the two never build different packages | 4.7d's assumed kill-at-InstallFiles mechanism |
| D-0210 | 2026-09-17 | 4.7d's interruption stays unproven and its check unreached rather than passing on an install that finished: a property on a missing volume fails in CostFinalize, and killing the client `msiexec` at InstallFiles does not abort the transaction, the service carrying it to `installed the product ... 0.0.3 ... status: 0` with no rollback (run 35267657646). A third method gets a measured experiment before another dispatch: the candidates are for installing over one another, so nothing about the shipped package is in doubt | D-0206's kill-at-InstallFiles interruption |
| D-0209 | 2026-09-17 | 4.7d asks Windows Installer which products are registered, through `RelatedProducts` on Package.wxs's UpgradeCode, because this install registers under HKLM's `Installer\UserData\<SID>\Products` while the HKCU Uninstall list it read holds nothing: run 35263524309 counted 0 installations as the app reported `0.0.2-upgrade`. install-lower counts too, so that query cannot first fire after an upgrade. MSI logs are UTF-16, hiding the rollback and the downgrade message, so a decoder reads them, and the uninstall names the product code | the HKCU Uninstall query; V-4.7d's uninstall by package path |
| D-0208 | 2026-09-17 | One script owns the pinned Windows KeePassXC download for ci.yml, release.yml and upgrade-desktop.yml, because a third copy of it drifted from the two the compat gate proves. `fetch-pinned-asset.sh` also reads its digest from stdin: coreutils escapes a name holding a backslash with a leading one, so every Windows destination path refused its own pinned bytes, which is how upgrade-desktop run 35260785520 failed. Its selftest now fetches to a backslashed path, red before the fix | ci.yml's and release.yml's inline KeePassXC downloads |
| D-0206 | 2026-09-17 | 4.7d proves the upgrade with the installed app's `--version` and a fixture made by the published CLI/MCP, since neither package contains the CLI. Its candidates are unpublished `0.0.1`, `0.0.2` and `0.0.3-upgrade` builds of one commit. The interrupted install ends `msiexec` while it copies files and passes only on a log naming the rollback, a missing-volume property having failed in CostFinalize before the transaction. A Linux downgrade is not-applicable. The harness runs only under `CI=true` or `KEYPASTE_UPGRADE_SANDBOX=1`: known folders ignore the environment, so nothing moves an MSI | V-4.7d's `--version` clause |
| D-0205 | 2026-09-17 | 4.7b closes on the `v0.3.1-rc.2` candidates instead of V-4.7b's `v0.3.1-rc.1`, because rc.2 is the first tag through 3.6b's signing steps and so the current packaging path. Until 4.4 ships, its approval check passes on a terminal-agent grant audited as method `prompt`, the app only pointing to that terminal. Refusal of a changed hash or attestation and of a missing prerequisite is proved by fixtures (13 and 33 cases), with no hosted negative run. The AppImage needs FUSE (`fuse3`), a documented Linux prerequisite | V-4.7b's rc.1 candidates |
| D-0204 | 2026-09-16 | Windows files keypaste compiles and the MSI are signed only by `sign-windows.sh`, which reads `signing.policy`: `none` signs nothing; `authenticode` refuses before any write without its five repository variables (endpoint, account, profile, client and tenant ID) or a dlib matching its pin, and runs after an `azure/login` OIDC step guarded by that policy. The dlib is pinned now (D-0142): Microsoft.ArtifactSigning.Client 1.0.128 by nupkg SHA-512 and x64 dll SHA-256. The rehearsal runs app.yml's own win-x64 steps behind a dispatch input with a runner-trusted root, uploading and attesting nothing | 3.6b's unpinned dlib |
| D-0203 | 2026-09-16 | The AppImage pins its runtime as well as appimagetool: without `--runtime-file`, appimagetool 1.9.1 downloads type2-runtime `continuous`, so type2-runtime `20251108` is held by SHA-256 in release-targets.json and always passed. The build runs from a script the matrix gate reads, and a pin restated outside the definition is refused. appimagetool validates AppStream only through `appstreamcli`, so it passes `--no-appstream` where that is absent. The pinned 1.9.1 asset reports continuous build 296 (`8c8c91f`) | D-0142's appimagetool-only pin |
| D-0202 | 2026-09-15 | A pointer move is activity only at a screen position the window has already seen, and leaving `Minimized` forgets that position, because Windows sends a move at the resting cursor on restore (F.13). `Touch()` past the deadline locks as `Idle` rather than extending, so input beating the activation re-check cannot revive an expired session (T-23). The handlers move into [ActivityWatch](src/Keypaste.App/ActivityWatch.cs), a seam like `App.Watch`, so a headless test runs what launch runs | `App.OnPointerMoved`'s position-blind touch; `Touch()` as two unconditional field writes |
| D-0201 | 2026-09-15 | The macOS desktop app moves to Expansion: Working proposition and R.1 release the Windows and Linux desktop apps and the CLI/MCP on all four current targets. 4.7a2, 3.5a, 3.5b and 4.10b move under a macOS desktop heading, and 4.7e takes the macOS clauses of 4.7b–d, including the Homebrew cask. The macOS CLI stays advertised, unsigned and un-notarized as disclosed, until 3.5b | macOS desktop and Apple signing in Working proposition, 4.7a and R.1; 4.7a's 4.7a2 Need; 4.7c's 3.5b Ships after; D-0149's cask at 4.7c |
| D-0200 | 2026-09-15 | F.2b2 closes on runner observation, observe-desktop run 35014816424 at `9b18750`. Its real-desktop record, a person's own minimize click on real macOS and Linux X11/XWayland desktops and the `Cmd+H` keystroke on macOS, moves to Expansion as F.2b3, Needs F.2b2, because no Working proposition journey waits on it | F.2b2's real-desktop Input |
| D-0199 | 2026-09-15 | F.2b2's runner checks drive a test-only observer, not a hook in the shipped app, because an unlock seam in a released binary risks user trust. It runs the real startup path and reads `App._session` and `_minimize` by reflection, refusing when either is missing, instead of composing through `App.Compose` and `App.Watch`. Its idle check is measured against the session's recorded deadline. Its workflow is dispatch-only; an unreachable check is unreached, and only a contradiction fails | — |
| D-0198 | 2026-09-15 | Further milestone moves: 5.2a–c, H.7, H.8, 5.7 and 5.4a–b join Working proposition with R.0f, 3.2 and 3.3; H.1–H.6, 7.1a–c and 7.2 join Paid release; 5.3a–d, 4.10a–b, M.1–M.3, 5.6 and 3.10b move to Expansion with Needs unchanged apart from D-0183. K.4, K.5 and P.0 drop to one line, their detail written when a row is picked | milestone placement of those rows |
| D-0197 | 2026-09-15 | Moved to Expansion with Needs unchanged, because no v1.4 journey exercises them: K.4, K.5, P.0, V.1a–b, V.3a–b, V.4a–b, V.5a–b, V.7, V.8a–b, V.9, 9.1a–f, 9.2a–b, 4.5, 4.3b, 4.4b, 4.6, 9.4, 3.10a, 1.5a, 8.1, 8.3a–e, 8.2b and 8.4a–b | their Working proposition placement |
| D-0196 | 2026-09-15 | 3.2 Ships after R.0f and founder daily use of that release instead of R.1, and 3.2 and 3.3 follow R.0f in Working proposition: the introduction describes the released CLI with its repairs, not the unfinished desktop journey | 3.2's Ships after R.1 |
| D-0195 | 2026-09-15 | R.0f is added directly after the repair rows, Needs F.6, F.9, F.10b, F.11 and F.12: the 0.3.0 CLI/MCP release publishes the repairs on `main`, and no provisional row could carry a release, R.0c having been 0.2.0 | — |
| D-0194 | 2026-09-15 | 4.5's draft review leaves R.1's release checklist and precedes the first gate or published claim relying on a threshold, because R.1 no longer needs 4.5 | D-0156's R.1 checklist item |
| D-0193 | 2026-09-15 | P.0's draft review moves from R.1's release checklist to P.9's: R.1 no longer needs P.0, and P.9 is the gate that claims coverage | D-0154's R.1 checklist item |
| D-0192 | 2026-09-15 | 10.2 Needs 7.2, H.5b, 5.5c and H.8 instead of 5.3a, H.5b, M.2c and 5.5c: the review covers the hosted drop relay, accounts, sealing, signing, revocation and billing, and sync and phone leave paid release | 10.2's 5.3a and M.2c Needs |
| D-0191 | 2026-09-15 | 7.2 Needs 7.1b and 7.1c instead of 7.1c alone: revocable links, attribution, team audit and policy govern shares through the hosted directory, over sealed and signed shares | 7.2's 7.1c-only Need |
| D-0190 | 2026-09-15 | 7.1c Needs H.4 and 5.4b instead of 7.1b: sealing to a recipient's key and signing use locally generated keys exchanged by any means, so its verifier must run with keys exchanged by hand and no directory. PRODUCT §5.4 keeps encryption and signatures free, so 7.1c ships free; its Paid release placement is priority only | 7.1c's 7.1b Need |
| D-0189 | 2026-09-15 | 7.1b Needs H.2 as well as 7.1a: the hosted team directory is made of accounts, which only publish members' locally generated keys | 7.1b's 7.1a-only Need |
| D-0188 | 2026-09-15 | 5.7 Needs 5.2c and 5.4b instead of 5.2c, 5.3c, H.5b, H.6 and H.9: it documents share, receive and relay operation for R.1, and sync guidance moves to 5.3d | 5.7's sync and recovery Needs |
| D-0187 | 2026-09-15 | 5.4b Needs 1.4c as well as 5.4a and 5.2b, and no longer Ships after R.2: a link receive hands the drop to the merge receive, and sharing by link is in the journey R.1 verifies. The previewed merge replaces quarantine import | 5.4b's Ships after R.2; quarantine import |
| D-0186 | 2026-09-15 | 5.4a Needs V.6 and 4.2 instead of V.1a: a share is protected by a generated six-word passphrase rather than a keyfile, and is created from the desktop as well as the CLI | 5.4a's V.1a Need and keyfile design |
| D-0185 | 2026-09-15 | H.8 deploys the hosted drop relay and no longer Ships after H.5b and H.6: drops hold no accounts, so account recovery and deletion gate nothing it serves, and R.1 needs it live | H.8's Ships after H.5b, H.6 |
| D-0184 | 2026-09-15 | H.6 Needs H.2 instead of 5.2b: team-plan export and deletion act on accounts and directory membership, not on synced vault blobs | H.6's 5.2b Need |
| D-0183 | 2026-09-15 | 5.3a Needs 5.2d instead of 5.2b: client sync needs the authorized blob storage now at 5.2d, not the drop endpoints now at 5.2b. It is the one Need changed among rows moved to Expansion; 5.3d gains the sync guide 5.7 no longer carries | 5.3a's 5.2b Need |
| D-0182 | 2026-09-15 | 5.2d is added at the end of Expansion, Needs H.4 and 5.2a, carrying the authorized blob sync and version retention 5.2b held, because no provisional row could; sync still needs trusted devices | — |
| D-0181 | 2026-09-15 | 5.2b serves drops and Needs 5.2a instead of H.4: upload, download, 1 MB and 7-day limits, delete-on-first-download and rate limits use no accounts or device keys | 5.2b's H.4 Need and sync scope |
| D-0180 | 2026-09-15 | 5.2c no longer Ships after R.1: the self-hostable relay is part of the journey R.1 verifies, so it ships before that gate | D-0164 |
| D-0179 | 2026-09-15 | R.3 Needs the team plan: H.3, H.5b, H.6, 7.2, 5.5c, 5.8 and 10.2. M.3, 5.6, 3.3 and 3.10b leave because the team release exercises no phone, signup list, community reply or sync guide, and H.10 already gates R.2 | R.3's Needs on H.10, 5.6, M.3, 3.3, 3.10b |
| D-0178 | 2026-09-15 | R.2 Needs only H.9, restore by redeployment, and Ships after R.1 and H.10: invited small teams share production env through the hosted relay over a dated observed period. Account, device, recovery, export and sync rows leave, because the pilot uses no accounts | R.2's Needs on H.3, H.4, H.5b, H.6, H.10, 5.3d, 5.7 |
| D-0177 | 2026-09-15 | R.1's Needs are exactly the §1.1 journey: 4.7c, 4.8, 4.9, E.1, 5.4a, 5.4b, 1.4c, 4.4, V.2b, 2.4, H.8, 5.2c and 5.7. V.2b is the ordinary mistake, since a receive keeps replaced values in history. Browser, import, TOTP, keyfile, backup, health, render, guide, compatibility, repository and parity rows leave; F.2b2 still gates 4.7c | R.1's former Needs |
| D-0176 | 2026-09-15 | PRODUCT v1.4: keypaste is credential sharing through encrypted files with agent access on the same vault. Small teams already share production env as plaintext files; a KDBX4 share of chosen entries under a six-word passphrase sent separately, in an expiring drop and merged by UUID with history, fixes that with no account. The team plan sells the directory and governance; sealing and signing stay free (§5.4). Drops are free within 1 MB and 7 days (§5.8). The introduction follows 0.3.0, not R.1 | v1.3's §1 order and §5.3 phone precondition; D-0090's gating of the community introduction on R.1 |
| D-0175 | 2026-09-14 | P.1 is verified against an emulated challenge-response fixture, and a run on a real hardware key is recorded as a dated observation, so owning a key is no longer an unstated condition of building it | — |
| D-0174 | 2026-09-14 | R.4 no longer Needs R.2; it Ships after R.2. Its verifier exercises the organization rows, not the consumer pilot, so R.2 decides only when an organization pilot may run (D-0132) | R.4's R.2 Need |
| D-0173 | 2026-09-14 | R.3 no longer Needs R.2; it Ships after R.2, which M.3 already ships after. R.3's release checklist gains owner approval of 5.8's drafted prices | R.3's R.2 Need |
| D-0172 | 2026-09-14 | R.2 no longer Needs R.1; it Ships after R.1. No external pilot begins before R.1, which is a publication gate rather than evidence R.2's verifier exercises | R.2's R.1 Need |
| D-0171 | 2026-09-14 | W.1 no longer Needs H.1: a browser KDBX prototype and the analysis of what a compromised delivery origin can reach use no hosted authentication. W.2a keeps H.2 | W.1's H.1 Need |
| D-0170 | 2026-09-14 | M.1 prototypes on the `macos-15` iOS simulator and an Android emulator; the run on real iOS and Android devices is a dated observation, so owning devices is no longer an unstated condition of starting it | — |
| D-0169 | 2026-09-14 | The phone route is Avalonia mobile over `Keypaste.Core`: one core under PRODUCT §4.3 and the desktop's UI stack (D-0044). .NET MAUI over the same core is the fallback only if M.1 measures autofill-extension memory limits Avalonia cannot meet. Native apps carrying a second KDBX implementation are rejected | — |
| D-0168 | 2026-09-14 | 5.8 no longer waits on owner-approved prices: every price stores `origin: draft`, a failing test keeps drafts out of Stripe live mode, the site and checkout, and its status reads built on drafts, review pending. Approval is an R.3 release-checklist item, not a step | 5.8's implied owner-approval dependency |
| D-0167 | 2026-09-14 | 5.5b no longer Needs 5.5a: stripe-mock and test mode need no activated account. 5.5a's restricted live key and webhook secret are its stated input, needed only for live mode | 5.5b's 5.5a Need |
| D-0166 | 2026-09-14 | 5.6 no longer Needs H.7: double opt-in and unsubscribe are verified against a mailpit fixture, and H.7's SMTP credentials are its stated input | 5.6's H.7 Need |
| D-0165 | 2026-09-14 | H.8 no longer Needs H.7: its deploy, backup and rollback commands are verified on a local Docker host, and H.7's host access, bucket keys and DNS record are its stated input | H.8's H.7 Need |
| D-0164 | 2026-09-14 | 5.2c Ships after R.1: it publishes a relay binary, and PRODUCT §1 puts Pilot publication after the Working proposition gate | 5.2c's publication without a gate |
| D-0163 | 2026-09-14 | Account failures: the account recovers by email plus recovery codes; the vault secret is unrecoverable unless the user configured a recovery keyfile; support never restores vault access (PRODUCT §2). H.5a documents costs within this | H.5a's open question |
| D-0162 | 2026-09-14 | Hosted authentication: the account password is verified server-side with Argon2id and never derives the vault key; MFA is WebAuthn or TOTP; each device holds its own keypair for H.4. Every part has mature libraries (§3.6, §3.9). OPAQUE is rejected for want of a mature audited .NET implementation, and passkey-only accounts for moving the recovery burden onto H.5a. H.1 writes protocols within this | H.1's open question |
| D-0161 | 2026-09-14 | 3.10a Needs only 4.2: it ships guides for shipped screens and a test failing on a screen without one, so each later screen's row brings its guide, as PRODUCT §4.8 requires. Its former Needs were a completeness gate, which R.1 already holds | 3.10a's Needs on 9.1f, 9.2b, 8.3c, 8.3d, 8.3e, E.1, V.4b |
| D-0160 | 2026-09-14 | 8.4b's submission and install checks run first on a self-distributed signed XPI and an unpacked CRX, and each store's review is recorded as a dated observation rather than waited on | — |
| D-0159 | 2026-09-14 | 8.4a builds stable extension identities and store packages without accounts; the Chrome Web Store registration and AMO API credentials are its stated input, needed only to upload | 8.4a's implied account dependency |
| D-0158 | 2026-09-14 | The shared approval interaction is the shipped terminal prompt: its fields, the line saying the reason was written by the agent, D-0084's sanitising and D-0027's two refusals, rendered identically on every surface. Per-surface designs are rejected because they drift | 8.2a's open question |
| D-0157 | 2026-09-14 | Merge semantics are KeePassXC 2.7.x's: UUID match, the newer modification wins and the other revision goes to history, `DeletedObjects` tombstones apply, and moves resolve by `LocationChanged`, fixtured from `keepassxc-cli merge` (law 4.6). Whole-entry last-writer-wins loses edits; three-way field merge needs a base KDBX does not store | 1.4a's open question |
| D-0156 | 2026-09-14 | 4.5 no longer waits on founder thresholds: tasks are measured by automated step and keystroke counts, every threshold stores `origin: draft`, a failing test keeps drafts from deciding R.1 or a published usability claim, and review is an R.1 release-checklist item | 4.5's implied threshold-approval dependency |
| D-0155 | 2026-09-14 | P.9 no longer Needs R.1; it Ships after R.1, and it Needs P.0 with no `origin: draft` remaining, because a coverage gate cannot rest on unreviewed dispositions | P.9's R.1 Need |
| D-0154 | 2026-09-14 | P.0 no longer Needs 0.2: the contract and its test run without KDBX code. Dispositions store `origin: draft`, the guide list derives from the recorded KeePassXC 2.7.12 tag SHA, a failing test keeps drafts from coverage claims and scope removal, and review is an R.1 release-checklist item. P.1–P.8 build on the drafted contract | P.0's 0.2 Need; its implied disposition review |
| D-0153 | 2026-09-14 | K.5's fork by another account is its stated input; its reader runs on recorded run JSON until then. V-K.5 records the one run approval `first_time_contributors` requires, and drops a secret-absence clause that fork runs cannot fail | K.5's implied fork dependency |
| D-0152 | 2026-09-14 | Fleet deployment targets Windows through Intune with a per-machine MSI variant, because a package exists and D-0139's per-user MSI cannot be assigned to a device | S.2's open choice of OS and management system |
| D-0151 | 2026-09-14 | S.2 Needs 4.7a1 rather than the 4.7a aggregate: a single named OS needs only its own package | S.2's 4.7a Need |
| D-0150 | 2026-09-14 | 3.9a Needs 4.7a2, the packaging it extends, and Ships after 3.5b: signing is a release requirement, not a build input | 3.9a's 3.5b Need |
| D-0149 | 2026-09-14 | 3.7a is the CLI formula only; the desktop cask moves to 4.7c, where the published DMG and its hash exist | 3.7a's desktop cask |
| D-0148 | 2026-09-14 | 4.7c Needs 4.7a; 3.5b and 3.6b move to Ships after, as signed downloads are RELEASE requirement 2, and the met 3.8 is dropped. It builds the publication path against fake-R2 fixtures, owns the desktop cask, and records the first real-release upgrade as a dated observation | 4.7c's 3.5b, 3.6b and 4.7a3 Needs |
| D-0147 | 2026-09-14 | 4.7d builds an unpublished synthetic pair of distinct numeric versions per platform, since D-0139 cannot order equal ones, so upgrade proof no longer needs further published releases; the real-release upgrade is an observation at 4.7c | 4.7d's implied dependency on published versions |
| D-0146 | 2026-09-14 | 4.7b no longer Needs 4.9 and E.1: installation exercises the screens its build contains, creating vaults through the CLI until GUI creation ships. The complete journey stays R.1's, which Needs both | 4.7b's 4.9 and E.1 Needs |
| D-0145 | 2026-09-14 | 3.5b no longer Needs 3.5a: signing, notarization and fail-closed paths are verified with an ad-hoc identity and a fake `notarytool` and `stapler`; 3.5a's Developer ID and App Store Connect key are its stated input, and nothing records a payload as signed before them | 3.5b's 3.5a Need |
| D-0144 | 2026-09-14 | 3.6b no longer Needs 3.6a: signing and fail-closed paths are verified with a certificate generated in the job and trusted only on that runner; 3.6a's Artifact Signing values are its stated input, and `signing.policy` stays `none` until they sign a candidate | 3.6b's 3.6a Need |
| D-0143 | 2026-09-14 | Windows signing uses Microsoft Artifact Signing through `signtool /dlib`, keeping D-0070, so the fixture and the real run differ only in arguments; an OV certificate in a cloud HSM is used only if Artifact Signing refuses eligibility | H-0017's route chosen at enrollment |
| D-0142 | 2026-09-14 | D-0140's test reads: pin the tool by content hash, through a hashed restore or a hashed download, and inspect the package with a tool that cannot lag it. The AppImage uses appimagetool 1.9.1, pinned by the SHA-256 of its tagged asset, and is read by the distribution's `unsquashfs` at the squashfs offset, never run; squashfs is a stable format. A `.deb` is distribution-bound and Flatpak needs a builder and runtime | D-0140's "hashed restore" wording |
| D-0141 | 2026-09-14 | F.2b2 is no longer BLOCKED: its checks run on `macos-15` and an Xvfb Linux runner as far as those sessions allow, and the real-desktop record covering only what a runner cannot observe is its stated input | F.2b2's BLOCKED hardware edge |
| D-0140 | 2026-09-14 | D-0139 rejected installers delivered as downloaded binaries in favour of a NuGet-pinned SDK. Inno Setup was the candidate: its installer can only be unpacked without running it by innoextract, whose releases lag Inno Setup's, so both tools would need pinning in step and a new Inno Setup could outrun the check that proves the payload. WiX is restored and hashed through NuGet, and `msiexec /a` ships with Windows. 4.7a2 and 4.7a3 apply the same test: pin the tool through a hashed restore, and inspect the package without running it using a tool that keeps pace with it | — |
| D-0139 | 2026-09-14 | The Windows installer is a per-user MSI from `WixToolset.Sdk` 5.0.2, pinned rather than tracked: v5 is MS-RL, and the maintenance fee starts in v6. Heat (`WixToolset.Heat` 5.0.2, lock-file hashed) harvests the payload instead of a generated list, because it runs from the Sdk-style project and needs no repository generator. `app.yml` pins the SDK nupkg's SHA-512. ProductVersion must be numeric, so the suffix goes in the file name, ProductName and summary info, and two candidates of one numeric version cannot be ordered. ICE38/ICE91 are suppressed | O-0015's Windows choice |
| D-0138 | 2026-09-14 | `release.yml`'s publish job attests every CLI/MCP asset and a manifest, and uploads the Sigstore bundle with them, so evidence outlives CI artifacts. Verification binds repository, workflow and tag. Preflight, gh 2.86.0 logged out, cli/cli v2.100.0: `verify --bundle` exit 0; changed byte, other `--repo`, other `--signer-workflow` exit 1; `--custom-trusted-root` works; no `--bundle` exit 4, asking for login. So download and check need no account. Attestation proves origin, not correctness or reproducibility (O-0012) | D-0116's record kept only as a workflow artifact |
| D-0137 | 2026-09-14 | The save gate is taken per attempt over an existing vault file and released before each retry sleep: red at 1113 ms of gate wait behind a sleeping save, green at 0; pool-probe 34846842996 found no hold across a sleep in 2,088 saves. A gated attempt re-reads after taking the gate, and a stranded temporary is swept before every release. Caller-visible: a save that queued behind an in-process commit and overwrote it now gets `VaultChangedOnDiskException`, as D-0119 intends. Residuals: a vault file created between the check and KeePassLib's own; the stamp is read after release | D-0136's hold to the end of a save |
| D-0136 | 2026-09-14 | The save gate is taken at the first attempt over an existing vault file, on every platform, and held to the end of that save: KeePassLib transacts only over an existing file, and only a transacted save uses D-0123's directory or `<vault>.tmp`, so doomed and first saves no longer queue: red at 1287 ms of gate wait, and pool-probe 34806033453 found 48 of 48 ungated, first interval ≤108 ms. A test pins the vendored rule. Residuals: a vault file created between the check and KeePassLib's own transacts ungated; the gate held across retry sleeps is F.12 | D-0123's serialisation of every save |
| D-0135 | 2026-09-14 | A mechanism a targeted test can reproduce is measured with that test; the full CI test command is reserved for the confirming run. CLAUDE.md's identical-command rule scopes that confirmation, and suite-load loops are for conditions only the suite produces, as F.9's starvation was | — |
| D-0134 | 2026-09-14 | F.10's overrun is queueing at `_saveGate`, which serialises every save though only transacted ones use D-0123's directory: doomed saves' first attempts took ≤5 ms, while 55 of 60 locally and 45 of 48 in pool-probe 34804202152 waited behind other saves' attempts and retries, up to 5012 ms. [SaveClock](src/Keypaste.Core/Internal/SaveClock.cs) times each part a caller waits for; the retry schedule bounds only the sleeps. F.10b gates only attempts that can transact | the sleeping-holder regression proposed for F.10b; STEPS' F.11 ID-table row, whose F.11a/F.11b children were never created (D-0133) |
| D-0133 | 2026-09-13 | F.11 is KeePassLib's `KdfPool` filling a static `List` without synchronization: concurrent first callers read a published count before its slot, or lose and repeat writes during growth, leaving a registry that fails every later vault. KeePassInterop's type initializer builds the registry once, and the runtime holds concurrent first callers until it returns; vendored source stays verbatim, as in D-0120. Every `PwDatabase` in `src/` is already built there, and shipped processes open their first vault on one thread, so no `0.2.0` disclosure is added | — |
| D-0132 | 2026-09-13 | PRODUCT v1.3: a step's Needs name only what its own code or verifier cannot run without. Time, human acts, milestone or release gates, freezes and convenience ordering are not dependencies. Gates decide what ships through Ships after; Human rows form their own track; recorded results are versioned or voided rather than frozen. STEPS Needs were re-derived under this rule, and ship semantics moved to Ships after or gate Needs | D-0090's milestone activation as a build prerequisite; demand-activated Scale |
| D-0131 | 2026-09-13 | Plan rows close on evidence, not elapsed time: 3.3 closes when every issue and comment since 3.2 is classified and answered, with security reports routed privately, and 6.1 loses its time box. PRODUCT §6.3's two-week sizing heuristic is unchanged | H-0007's fourteen-day feedback period; 6.1's two-day bound |
| D-0130 | 2026-09-12 | [verify.sh](scripts/verify.sh) owns local verification and CI preparation, including the separate desktop/CLI consistency project; workflow lint precedes builds. Replayable probe readers reject incomplete evidence. Diagnosis requires a distinguishing measurement before choosing a repair; independent ready work may continue during external waits while retaining its gates. These delivery changes implement PRODUCT's existing engineering laws | CLAUDE's backend-only full-suite checklist and commit-once rule applied to unfinished experiments |
| D-0129 | 2026-09-12 | F.9 had two worker-pool defects. The bridge uses synchronous `Connect` on a dedicated thread, preserving its 500 ms budget: `ConnectAsync` queues its loop, allowing `CancelAfter` to expire before trying a running approver. The test harness uses in-memory `Pipe` streams because anonymous pipes blocked a worker per outstanding read. Test projects use `Microsoft.Diagnostics.NETCore.Client` to dump stalled hosts because .NET 10 createdump no longer accepts a pid | D-0126's instrument as the whole reading |
| D-0128 | 2026-09-12 | F.9 is MCP worker-pool supply: [pool-probe](.github/workflows/pool-probe.yml) run 34701431621 on `windows-2025` failed 16, 8 and 0 of 80 runs at worker floors 1, 4 and 256; ten MCP tests overran. Save failures split to F.10 because the intervention left 4, 4 and 2 of 80, with sleeps up to 2482 ms against 2240 ms requested. Its first interval combines gate wait and save work, leaving indirect pool effects unresolved | D-0125 and D-0126 on starvation as inferred; F.9 as one finding |
| D-0127 | 2026-09-12 | Cloudflare Git integration deploys keypaste.com from `site/`, keeping its deployment credential in Cloudflare. `site.yml` was deleted after all four runs failed to deploy. [verify-site-disclosure.sh](scripts/verify-site-disclosure.sh) checks the origin manually; its `--selftest` remains in CI. [verify-release-matrix.sh](scripts/verify-release-matrix.sh) checks pages at the source ref, correcting D-0121's overstated live-deployment guarantee | D-0121's deploy from CI |
| D-0126 | 2026-09-11 | The `0.2.0` disclosure records F.9's observed symptom; its mechanism and repair remain open. [PoolSnapshot](tests/Keypaste.Core.Tests/PoolSnapshot.cs) samples during waits from a dedicated thread because assertion-time pool readings miss the stall. It separates timer lateness from queue latency: one local failure had a 50 ms delay arrive 941 ms late while a queued item started in 8 ms. On 24 cores, one of ten starved and none of ten either way was inconclusive | — |
| D-0125 | 2026-09-11 | Keep `ApproverConnection.ConnectTimeout` at 500 ms: [listing-probe run 34653284139](.github/workflows/listing-probe.yml) observed 5834 ms against a bound listener, proving the existing deadline was unenforced. Four waits across three subsystems overran five- to elevenfold in the same runs. Their contention suggests thread-pool starvation, which F.9 must measure before selecting a repair | D-0124's account of F.8 as unrepaired and its cause unknown |
| D-0124 | 2026-09-11 | Only [ListingCall](tests/Keypaste.Mcp.Tests/ListingCall.cs) reads listing content; its factory calls `list_entry_names` so credential-release results cannot reach its diagnostics (law 3.5). Retained CI logs contain transcript counts and audit methods only. Timing the call separately from the test identified F.8 on its first reproduction | a diagnostic taking a `CallToolResult`; a transcript tail in a run log |
| D-0123 | 2026-09-11 | A save writes its transacted temporary into a directory private to the process, redirected once and never swapped, because `TMP` is process-wide: a per-save swap would be read by any other thread wanting a temporary file, and the runtime and Avalonia both want one. Saves are serialised in-process, since one directory cannot separate two of them; a child of `keypaste run` is handed the original `TMP`/`TEMP`; an abandoned directory is swept by whether its owner file still opens | *supersedes:* D-0114's rejection of a per-save temporary directory |
| D-0122 | 2026-09-11 | F.6 is `KeePass_TxF_` colliding with its own 8.3 alias: build 10.0.26100 refuses a create whose `KEEPAS~1.TMP` another transaction holds, where 20348 advances to the next alias. 1387 of 1600 concurrent saves refused, and 0 with a directory each, in [txf-probe.yml](.github/workflows/txf-probe.yml) runs 34619717595 and 34620670405. Measured on Server 2025; Windows 11 24H2 shares the build and is inferred. `PwDefs.ShortProductName` is `KeePass`, so keypaste collides with KeePass 2 as readily as with itself | *supersedes:* D-0114's exclusion of 8.3 aliasing |
| D-0121 | 2026-09-11 | A qualifying `main` push was intended to deploy keypaste.com after source checks and then verify the origin. This followed f69c393, whose 0.2.0 revert disclosure remained absent from the live page for a day despite green gates. The workflow never deployed: its environment lacked `CLOUDFLARE_API_TOKEN`, all four deployment runs failed and all three subsequent live checks were skipped | D-0117’s "because the site deploys by hand" |
| D-0120 | 2026-09-11 | The file KeePassLib strands beside the vault when its fallback move is refused is swept by keypaste, leaving the vendored source verbatim: opened at one exact path with `FileShare.None` and `DeleteOnClose` rather than deleted by name, so a writer on the non-transacted path, where that name is the live write target, refuses the cleanup and remains untouched. Residual: its close-then-rename window, where the vault is already deleted. A test pins our restated suffix to `FileTransactionEx.StrTempSuffix` | deleting the stranded file by name, and a `KEYPASTE_*` guard in vendored source |
| D-0119 | 2026-09-11 | A save is retried on a native move refusal only for a code a probe recorded on a named platform (6800 on Windows 10 Pro 19045), never on `Win32Exception` as a type, since the vendored fallback raises it for a full disk too; and the vault is re-read across every wait, because the name a save waits for is usually another process saving it, and outlasting that writer reverted it with no history item. V-F.7 as written would have codified that revert. Residual: a hold outlasting one wait and committing inside the next attempt | V-F.7, and D-0017 leaving the changed-on-disk check outside the retry |
| D-0118 | 2026-09-10 | Clean-machine evidence for the CLI/MCP patch is a fresh GitHub-hosted runner per target, driven by [install.yml](.github/workflows/install.yml) with a `version` input that targets any `published[]` entry so an unadvertised candidate is exercised before a page names it, and each leg now creates a vault and injects into a child rather than only printing a version: the documented install path is `curl` plus a checksum, which never engages Gatekeeper or SmartScreen, so the double-click prompts stay with 4.7b and the desktop packages that actually meet them | R.0c owning the download-and-double-click check |
| D-0117 | 2026-09-10 | What the advertised download is known to get wrong is recorded in [release-targets.json](release-targets.json) against the version that carries it, and named on every page in `defects_disclosed_in`: [verify-release-matrix.sh](scripts/verify-release-matrix.sh) refuses a page that drops a defect and a page still naming one belonging to a release no longer advertised, so the block cannot outlive the version it describes, and [verify-site-disclosure.sh](scripts/verify-site-disclosure.sh) asks keypaste.com, by hand after a deploy (D-0127) | the download pages saying only that `v0.1.0` lacks `setup` |
| D-0116 | 2026-09-10 | R.0b is the completion record and anonymous verification of public bytes, and nothing about origin authenticity: the manifest's signature/provenance references move to 3.8, which owns them and is where they can actually be checked, leaving V-R.0b's three refusal fixtures as the substance and reachable without a tag or a credential | the per-component manifest carrying signature/provenance references |
| D-0115 | 2026-09-10 | Build attestations stop gating the CLI patch: 3.8 leaves R.0c's Needs and keeps 4.7c's, so provenance still arrives before any public desktop download and before R.1, but no longer stands between a `v0.1.0` user and F.1b, where `env export --force` deletes the vault. Attestation authenticates origin and cannot make a binary correct; `v0.1.0` shipped without it and SECURITY.md already discloses that provenance verification does not exist | 3.8 as a prerequisite of R.0c |
| D-0114 | 2026-09-09 | The account of why a concurrent save fails is withdrawn and nothing is repaired on it: `ERROR_TRANSACTIONAL_CONFLICT` attaches to a name and never to a directory, [scripts/txf-probe.cs](scripts/txf-probe.cs) excludes every mechanism reachable from this machine, and the eight-attempt budget stands on what a runner needed while F.6 waits on [txf-probe.yml](.github/workflows/txf-probe.yml) on `windows-2025` | D-0107's account of the mechanism, and F.6's Build line resting on it |
| D-0113 | 2026-09-09 | A pipelined `tools/call` is waited for rather than refused as a client that never introduced itself, bounded to one second in [AwaitCompletionAsync](src/Keypaste.Mcp/McpAudit.cs): a request is still never answered for a client that cannot be named, and a third occurrence re-opens F.5 rather than widening the grace | treating a pipelined tool call as a client that never introduced itself |
| D-0112 | 2026-09-09 | Three set variables are not a working credential, so the guard runs [publish-release.sh](scripts/publish-release.sh) `--check` against R2 on any dispatch that has the secrets, while a dispatch's `-dryrun` version says nothing about the real prefix, which the tag still checks | the presence of three secrets as the guard's whole answer about publishing |
| D-0111 | 2026-09-09 | A gate does not ask whether a file contains a string through a reader that exits early, because under `pipefail` the writer takes SIGPIPE and a present string reads as absent (release dispatch 34400224237); `code_has` matches with `case` instead, and the same idiom is left in place at `verify-demo.sh` and `verify-mcp-stdio.sh` until one of them is observed to fail | the assumption that `grep -q` at the end of a pipeline reports what a file contains |
| D-0110 | 2026-09-09 | Extract tag-only changelog lookup, tag/source comparison and publication allowlist checks into scripts exercised by [verify-release-preflight.sh](scripts/verify-release-preflight.sh) on every push. [RELEASE](docs/RELEASE.md#what-only-a-tag-reaches) records the operations that still require a tag | the changelog lookup, version comparison and publication allowlist as step bodies |
| D-0109 | 2026-09-09 | A decision only a tag can reach belongs in a script a fixture can drive: the green-gate check is [require-green-gates.sh](scripts/require-green-gates.sh), driven through a fake `gh` by [verify-green-gates.sh](scripts/verify-green-gates.sh) on every push, with the required set read from `release-targets.json` so a component with its own gate is required without an edit | the green-gate check as a step body in release.yml's guard |
| D-0108 | 2026-09-09 | What keypaste ships is one checked file: [release-targets.json](release-targets.json) holds the advertised targets, their floors and an append-only `published` list that the workflows build `fromJSON`, `published` is held against git history and so needs `fetch-depth: 0`, `targets` and `source_only` must stay disjoint, and what a `floor_evidence` value means is owned by [docs/RELEASE.md](docs/RELEASE.md#floor-evidence) | the four-target list in `release.yml`'s matrix, asset count and allowlist as independent sources |
| D-0107 | 2026-09-09 | A save is retried eight times over about 2.2 seconds, a budget set from what a runner needed rather than from a reproduction and now standing for a reason not yet known, since D-0114 withdrew the mechanism and F.6 owns finding it; `UseFileTransactions = false` stays rejected because `bTransacted` false writes over the live vault, and the wait holds a thread-pool thread, so the budget is a ceiling to watch rather than grow | D-0017's account of why a save is retried |
| D-0106 | 2026-09-09 | The destination check no longer trusts an exit code to mean "no answer", because jq 1.6 on ubuntu-22.04 exits 0 on empty output and an empty answer read as a free destination: `keys_at` requires digits or refuses, and the fixture carries the two cases that reach past the positive control | F.4a's reliance on `jq -e`'s no-output exit status |
| D-0105 | 2026-09-09 | Scripts invoked directly by workflows must be committed executable. [WorkflowScriptsAreExecutableTests](tests/Keypaste.Core.Tests/WorkflowScriptsAreExecutableTests.cs) reads the index mode through `git ls-files -s` because `core.filemode` is false on the development machine; filesystem permissions cannot establish the committed mode | the assumption that a committed script is a runnable one |
| D-0104 | 2026-09-08 | A release destination is refused unless a listing positively says it is empty, using an `s3api list-objects-v2` body, paired with a positive control that must find an already-published prefix non-empty, with the upload in the same script as the check so zero write calls can be counted; the window between the check and the first byte is narrowed by serialised `concurrency` and not closed | the `|| true` destination check; the claim in DECISIONS' infrastructure table that the pipeline refuses to overwrite a version that already exists |
| D-0103 | 2026-09-08 | A release too large for one frame is refused where it is encoded and recorded as `Undeliverable` against a `denied` decision, so an approval is never written up as a failure to ask: a credential is refused whole and never trimmed, and the grant is stored anyway because the size belongs to the entry rather than to the request (T-11) | `Encode(CredentialReply)`'s unboundedness; the `denied`/`failed` "the approver could not be asked" record on this path |
| D-0102 | 2026-09-08 | One listing reply is bounded by encoded bytes in `ApproverProtocol.Encode(NamesReply)`, since only the writer owns the escaping policy: names are dropped whole from the end, an optional `complete` flag says so, no total or omitted count is reported because no layer can compute one honestly, and the reply still takes no arguments (T-4) | THREATS.md T-1's "1000 entries per listing" cap; `ListEntryNamesTool.MaximumEntries` and its `exposed.Count == MaximumEntries` truncation claim |
| D-0101 | 2026-09-08 | A concurrent request is refused where it arrives rather than only where it is decided, and answered `BUSY` rather than `DENIED` so an agent does not read "not now" as a person's no; a boundary refusal cannot arm the F.3a denial cooldown, whose only writer stays inside `AskAsync`, and contention at the inner guard throws instead of returning a retryable failure (T-11) | THREATS.md T-11's claim that the bridge refuses a second request; the `bool Reachable` outcome pair |
| D-0100 | 2026-09-08 | Approval lifetimes use both clocks: `HasExpired` chooses the greater elapsed time so rollback or suspend ends a grant; `StillInForce` chooses the lesser so a forward clock jump cannot end a denial cooldown early. [Deadline](src/Keypaste.Core/Approval/Deadline.cs) forgets unconditionally by reference (T-12, law 3.7) | the claim that a TTL is a wall-clock instant; `GrantCache.Expire`'s deadline re-check |
| D-0099 | 2026-09-08 | The master-password field is held to a differential accessibility check rather than a peer-type assertion: two passwords of the same length sharing no character must produce an identical automation surface, which is true only if nothing on it is a function of the characters (T-22) | T-22's "proved by an implementation"; SECURITY.md's claim that pasting a master password is supported |
| D-0098 | 2026-09-08 | A clipboard write the windowing system has not finished is given back rather than installed, with the countdown's operations queued through one ownership hand-off: `Take` ends the countdown synchronously, quit is the one caller that waits, and a plain `keypaste run` line is owned by nobody so one landing after a lock stays where it landed (law 3.7) | D-0046's fire-and-forget disposal clear; the assumption that a synchronous fake exercises a fire-and-forget path |
| D-0097 | 2026-09-08 | keypaste's binaries publish as keypaste with `Company` set rather than inherited from `Authors`, and vendored code keeps upstream's notice and never gains keypaste's, as GPL-2.0 requires; the copyright line carries no email address, and the rule is forward-only because `v0.1.0` is published and immutable (D-0036) | the unwritten `Authors`-to-`CompanyName` fallback; the blank copyright on the vendored assembly |
| D-0096 | 2026-09-08 | Minimizing the window locks the vault by calling the same `AppVaultSession.Lock` as `Ctrl/Cmd+L` and the idle timeout, with the setting read at the minimize rather than captured at launch and only the transition into `Minimized` acting, so restore shows the locked screen instead of being a way back in | D-0095's inert minimize-lock; the claim that a persisted preference is an applied one |
| D-0095 | 2026-09-08 | The desktop reads `app.toml` once, at composition, and the Settings screen shows that same record rather than a second read: a timeout the list does not offer is honoured and named rather than snapped or written back, and `AppSettings` has already clamped it into a range that locks (D-0028, law 3.7) | — |
| D-0094 | 2026-09-08 | A joined entry path is resolved once by one resolver shared by `Find(string)` and `RemoveEntry(string)`, and a path two entries answer to is refused on the way in as well as on the way out; creating the first collision stays allowed and narrated, because KeePassXC makes them and refusing would put keypaste at odds with a file they both hold (law 3.7) | D-0091's `Vault.Find(string)` first-match contract; `add`'s joined-path duplicate check |
| D-0093 | 2026-09-08 | `keypaste env pull` deletes the file it imported and never whatever is at that path when it finishes, binding the offer to a [SourceSnapshot](src/Keypaste.Core/SourceSnapshot.cs) of the canonical path frozen at read time plus a SHA-256 of the bytes: the removal renames the file beside itself first, then hashes and deletes something no other writer can reach, and a mismatch puts it back rather than tidying either copy away | the claim that a path is enough to identify the file a command read; `File.Delete` on a vanished source reporting `Deleted` |
| D-0092 | 2026-09-08 | `env export` refuses a destination that is the vault it reads from and `--force` cannot lift it, because the guard takes no such parameter; path identity is one rule in [PathIdentity](src/Keypaste.Core/PathIdentity.cs), and since it cannot see a hard link, a bind mount, a `subst` drive or an 8.3 name, any KDBX destination is refused as the wider fail-closed answer | the claim that `Path.GetFullPath` is enough to tell one file from another; `UnlockViewModel`'s case-insensitive comparison on every platform |
| D-0091 | 2026-09-07 | An entry is addressed by its group path and its title, as [EntryName](src/Keypaste.Core/EntryName.cs), never by the two joined, with all mutation through one locator that walks the tree the way the listing does; a name two entries answer to is refused rather than resolved to whichever came first (law 3.7) | the claim in `EnvStore.Remove` that a path built through `EnvConvention` cannot reach outside the project group |
| D-0090 | 2026-09-07 | PRODUCT v1.2 accepts an everyday KeePass-compatible password manager with environments, controlled agent access, managed hosting and organization-owned credentials. STEPS is gated Working proposition → Pilot ready → Paid release, public distribution and daily-use recovery before hosted reliance, pricing separate from delivery milestones, and account recovery never implying vault decryption | D-0061 audience/order, D-0062 first-user boundary, D-0063 team scope, D-0065 payment sufficiency, D-0069 mandatory no-GUI launch copy, D-0074/D-0077/D-0079 tier and pickup rules |
| D-0089 | 2026-09-06 | The repository went public by replacement rather than by purge: pushing `main` to a fresh `notinferred/keypaste` and deleting the old one removed every `refs/pull/*` ref, both Claude-authored commits and the pre-rewrite identity at once, kept the URL, and cost only the three R2 secrets to re-add; the old repository is mirrored off-repo | D-0082's price for a fresh repository; 3.0's Support-ticket route |
| D-0088 | 2026-09-06 | `SanitizeProse` exempts the path separator and only the separator, so an ordinary request stops firing the warning that the reason was scrubbed while markup, fences, pipes, braces and brackets still never reach a reason; the audit line records the resolved entry name rather than the opaque handle | the claim that `verify-demo.sh` covers the reason and entry fields |
| D-0087 | 2026-09-05 | Commits are authored `keypaste <contact@keypaste.com>`, set globally so it is the default rather than something to remember per repository, and it is forward-only: the commits already published carry a personal identity that no configuration change reaches (law 3.8) | the author identity on every future commit |
| D-0086 | 2026-09-05 | CI runs on GitHub-hosted runners because the repository is public and they are therefore free; every step keys off `runner.os` rather than the label, which is what makes the move cost nothing, and this reopens `osx-x64` (O-0013) while leaving a second provider's second opinion lost | D-0042's runner choice; K.5's reason to exist |
| D-0085 | 2026-09-05 | `keypaste setup` calls each AI client's own command (`claude mcp add`, `codex mcp add`) to preserve that client's configuration ownership, because `~/.claude.json` carries the user's login state and is rewritten continuously by a running client; a client with no such command gets its block printed for pasting, and any previous entry is cleared before adding so re-running fixes a moved vault | the MVP tier's claim that nothing new is built in it |
| D-0084 | 2026-09-05 | Hostile review before publication (10.1): untrusted names draw through `EntryNameSanitizer` everywhere they are rendered while what addresses an entry or feeds the clipboard stays unscrubbed, both MCP tools' catches are total so no access can happen without an audit line, and the approval prompt says when the name or reason drawn is not the one stored; the approver still writes no audit line of its own, which T-14 now states (D-0020) | — |
| D-0083 | 2026-09-05 | Remove the citation gate, CI classifier, run cancellation on `main`, recording pipeline, K.3 and launch precondition list. Decisions become one-line records for architecture, security or money; ideas stay one line and the archive is frozen | D-0043's rule, D-0078, D-0080, D-0033's recorded-cast requirement, D-0051's document ceremony |
| D-0082 | 2026-09-05 | There is no names step: the repository is `notinferred/keypaste` and stays there, the organisation exists empty only so the name is not taken, and npm is not sought because nothing ships through it (D-0053) | the names row 0.4 and its human action, the registration half of D-0053 |
| D-0081 | 2026-09-05 | `app.yml` is the app's release gate: a `v*` tag packages `keypaste-app` for `linux-x64`, `win-x64` and `osx-arm64`, and every archive is held to `--version` equalling the tag and to a `--selftest` that round-trips a vault through `Keypaste.Core` before it is packaged and hashed; the archives stay workflow artifacts, because the free binary is the signed one (D-0070) | the `--selftest` that was one `WriteLine`; the app workflow that had no tag path |
| D-0080 | 2026-09-04 | The citation gate gets a workflow that its own inputs can trigger: `docs.yml` runs `scripts/verify-claims.sh` on one Linux runner with no SDK and no restore, on pushes touching the documents `ci.yml`'s `paths-ignore` excludes. Check A had been widened to scan every document while the paths it reads stayed ignored, so changes to those documents never triggered their own check | D-0078's `paths-ignore`, and the claim in `CLAUDE.md` that no gate reads the governance documents |
| D-0079 | 2026-09-04 | `[MVP]` is the CLI launch, not the app's: 3.5, 3.6 and 4.7 move to `[Launch]`, and 2.4b and K.5 join `[MVP]`. Neither developer account is held and Azure's individual eligibility is a review that can refuse, so the old definition made the tier unreachable while D-0069 had already decided the CLI launches without a GUI. The pickup rule now skips BLOCKED rows and the header lists them | D-0077's MVP definition |
| D-0078 | 2026-09-04 | `ci.yml` runs each job only when the push touched what that job can catch: `scripts/ci-scope.sh` classifies the diff; pinned-page edits get a Linux build and the demo gate; the three-OS test, the KeePassXC matrix and the AOT publish each carry one relevance `if:`; pull requests, tags and dispatch always run everything; the compat expression is pinned by `CompatGateIsPermanentTests` | D-0008 "on every push" |
| D-0077 | 2026-09-04 | Scope cut: `[MVP]` is what a stranger can install this month: names, public repository, GIF, launch and the released, signed app; the first dollar (4.7 + 5.2) moves to `[Launch]` with 1.4, 4.3 and 4.4; the ideas and not-in-git tables fold into this file and the long-form records become its archive | D-0065's tier placement, D-0074 |
| D-0076 | 2026-09-04 | `global.json` rolls forward `disable`, not `latestPatch`: an SDK patch moves the implicit ILCompiler and ILLink versions the lock files pin, so CI was red for nine days with no code change; SDK bumps are deliberate and regenerate the lock files | D-0002's roll-forward choice |
| D-0075 | 2026-09-04 | §1 names both words: a password manager and a secrets manager. Env injection already is one for a single person; the team half arrives with 7.1 and 7.2 and is priced then, not now | §1 wording of D-0061 |
| D-0074 | 2026-09-04 | Nothing is gated on "revenue" or "benchmarks" any more; every step in `docs/STEPS.md` carries a Verify line and a tier tag, and a tier starts when the one before it has shipped | the "Gated" section of `docs/STEPS.md` |
| D-0073 | 2026-09-04 | 4.5 (UX bench) and 4.6 (headless render gates) leave the parking lot at the app's first release, because that is when there is somebody to measure | D-0043's parking of both |
| D-0072 | 2026-09-04 | Business figures live in `keypastebusinessnotes.md`, in the tree and ignored by `*businessnotes*.md`; committed pages carry shapes and decisions, never numbers | makes the Not-in-git table in `DECISIONS.md`'s row true; D-0006 unchanged |
| D-0071 | 2026-09-04 | KPIs, none from telemetry: downloads per version (R2 logs), list signups, stars and open issues once public, paying subscribers, MRR, monthly churn, weekly active synced vaults (distinct blobs updated in 7 days, server-side count), launch reply latency; targets internal | — |
| D-0070 | 2026-09-04 | Windows binaries are signed with Azure Trusted Signing from the app's first release; macOS notarized per D-0057; the free binary is the signed one (law 5.4) | D-0057's Windows half |
| D-0069 | 2026-09-04 | The CLI and agent launch as soon as 3.1 and 3.2 close, with the copy saying "no released GUI"; the app gets its own release and its own post | — |
| D-0068 | 2026-09-04 | Browser autofill, importers, TOTP and an SSH agent are `[Launch]` steps (8.1, 8.3, 9.1, 9.2, 9.3), not ideas; the condition in D-0059 never fired and is overtaken by the re-ratification | D-0059 |
| D-0067 | 2026-09-04 | Data sources, in the order they are built: the local KDBX file; the hosted encrypted blob; the running agent plus `policy.toml` plus the audit log; external OAuth grants (`[Scale]`) | — |
| D-0066 | 2026-09-04 | The headline number is "what can act as you right now": live agent connections + unexpired grants + standing `[[allow]]` rules, read from the agent over the pipe, each shown beside the sum; "nothing listening" is its own state and never renders as zero | the 4.3 prompt's wording |
| D-0065 | 2026-09-04 | The first dollar needs both the released desktop app (4.7) and hosted sync (5.2); nothing is charged before both exist | — |
| D-0064 | 2026-09-04 | `keypaste-relay` is one .NET NativeAOT binary over S3-compatible blob storage with SQLite for licences; hosted and self-hosted run the same binary; share links are a one-download bundle endpoint on it; billing is Stripe Checkout → licence key → relay check, and nothing in any client is gated | the 5.0, 5.1 and 5.2 prompts |
| D-0063 | 2026-09-04 | Three tiers, recorded internally and published nowhere: Free (the whole password manager, self-hosted relay included), Individual (hosted sync and shares), Team (per seat, shared env sets and the broker); figures in the working file | — |
| D-0062 | 2026-09-04 | The first buyer is a solo developer paying for hosted sync; the first user is anyone who keeps a `.kdbx` | — |
| D-0061 | 2026-09-04 | `docs/PRODUCT.md` re-ratified as v1.1: a KeePass-compatible password manager, freemium, hosted zero-knowledge sync with self-host first-class, the agent bridge as the selling point; §3 byte-identical to v1.0 | v1.0 §1, §2 "for everyone", §4.2 CLI-first |

## Ideas

Keep ideas to one line and remove shipped or rejected rows. Promotion to STEPS requires a falsifiable Verify line. Financial figures stay outside git.

Statuses are open (undecided), parked (deferred with a reason), rejected, promoted (a STEPS task) and shipped.

### UI direction

Steps 4.1 and 4.2 adopted a calm, precise interface with whitespace, a muted palette, one accent, Inter, subtle depth and full dark-mode support. Reserve warnings for exceptional actions; avoid terminal styling, repeated padlock icons and dense tree tables. The approval dialog establishes the identity by showing who requests what and why.

| idea | who | status | why |
|---|---|---|---|
| The listing after an upload is printed, not checked | agent | open | `publish-release.sh` re-lists the prefix it just wrote and only prints it, exactly as the step it replaced did. Turning it into a count that must match what was uploaded needs a claim about R2's list-after-write consistency this repository has not established, and a false failure there would burn a version that had in fact published. R.0b owns the completion record and is where it belongs.
| A policy allowance is spent on a release that was never delivered | agent | open | `PolicyGate.Evaluate` takes the hourly allowance at decision time, and F.3d's undeliverable refusal happens after it, so a rule burns one release per TTL for a value that can never arrive. Refunding it means telling the limiter that a decision it already recorded did not happen, which is a shape the sliding window does not have today. Wants its own task and Verify line. |
| `EntryExposure`'s glob error reaches a refusal reason with no cap | agent | open | `ApproverHandler` includes an operator-supplied parse error in its refusal. F.3d preserves the method and connection by replacing oversized text with a constant message, losing the useful diagnostic. Bounding the error at its source needs a separate task and verifier. |
| The approver cannot say why a connection ended | agent | open | `ApproverListener.ServeAsync` catches peer disconnects, write failures and handler exceptions together, without a reporting path. An unwritable reply silently lost connection grants until F.3c exposed it two stages later. Distinguishing these failures needs a separate diagnostic task on the unlocked-vault process. |
| One-time encrypted share links, self-hostable relay, key in the URL fragment | founder | promoted | 5.4a/b own the reviewed key-transfer design, bundle endpoint and quarantine import; 5.2 owns the underlying relay. O-0021 (a bundle is untrusted input) is answered before the sharing endpoint ships. |
| `keypaste merge`: entry-level reconciliation of two KDBX files by UUID | founder | promoted | 1.4a–1.4c. Local reconciliation and managed sync both need recoverable divergent edits; the external-write refusal in D-0050 is only the existing safety boundary. Sharing can reuse the same engine. |
| A failed listing records itself as `no-approver`, which is a different thing from "the source threw" | agent | open | Found while adding the busy method beside it in F.3b and deliberately not fixed there. `ListEntryNamesTool` files every `VaultAvailability.Failed` under `AuditMethod.NoApprover`, so a vault on a disk that went away reads in the log as nobody having been running an approver. Wants its own word and a Verify line; the credential path already distinguishes the two. |
| GitHub private vulnerability reporting as a second channel | founder | open | `security@keypaste.com` is tested and works, and law 3.10 asks for *a* private contact, not two. One toggle if it is ever wanted. |
| Multi-vault and vault-per-project ergonomics | founder | promoted | P.2 extends the current recent-vault list; accepted KeePassXC coverage under D-0090. |
| Git-friendly vault workflows and conflict guidance | founder | open | No prompt exists for it. Blocked on step 1.4 for merging concurrent KDBX edits. |
| Hosted relay and sync as the first paid offering | founder | promoted | Step 5.2 and the Pilot ready/Paid release gates in STEPS (D-0090). Convenience never security (law 5.4); relay implementation alone is insufficient evidence to charge. |
| Delegation dashboard: aggregate agent grants, MCP connections, external OAuth | founder | promoted | Steps 6.1 and 6.2. External grant feasibility must be demonstrated before promising provider coverage. |
| Teams: organization-owned credentials including shared env sets (the copy model) | founder | promoted | Step 7.1 and its team prerequisites. Key handling requires review against PRODUCT §3; revocation bounds future access and cannot erase past copies. D-0090 broadens ownership beyond environments. |
| Teams: a broker that grants controlled credential access (the access model) | founder | promoted | Step 7.2 reuses the approved-release core. Revocation denies the next broker request; retained static credentials remain usable until the provider rotates or expires them. |
| Team SSO for the hosted service, never on the vault path | founder | promoted | Step 7.3. Account authentication and provisioning do not inherently confer vault decryption authority. |
| Team delegation dashboard | founder | promoted | Step 7.4; depends on the underlying membership, broker and audit contracts. |
| "Design language: modern, calm, trustworthy" as a checklist item | founder | rejected | Nothing about it can fail, so it was never a step. The direction above is the durable form. |
| Managed onboarding with account creation before file management | founder | promoted | The Pilot ready flow may create an account first; local use stays account-free. D-0090 removes the user-managed-file requirement without allowing server decryption. |
| Local-first and managed encrypted sync | founder | promoted | D-0060 keeps server-side decryption out of scope; D-0090 permits managed onboarding and distinguishes account recovery from explicitly configured vault recovery. Step 5.2 and the hosted gates own delivery. |
| TOTP/2FA storage with code-only agent release | founder | promoted | Steps 9.2a and 9.2b; provisioning and display must preserve the interoperable parameters. |
| Command palette (Ctrl/Cmd+K) | founder | open | Fits the keyboard-first shell 4.1 shipped. |
| Onboarding that offers creation and existing-vault import | founder | promoted | 4.8 and 9.1a–9.1f cover newcomers and existing vault users without terminal/file-format expertise. |
| SSH key management and a `keypaste ssh` agent integration | founder | promoted | Step 9.3; integration behavior must be compared explicitly with the KeePassXC baseline. |
| Bound `PolicyRateLimiter`'s hourly windows on both clocks | founder | open | Same exposure D-0100 closed for grants and cooldowns: an allowance measured on the wall clock alone comes back early when the clock moves forward. Its own task, with its own Verify line. |
| Git hooks that block committing plaintext secrets | founder | open | Could stand alone; free marketing. |
| Secret leases for long-running agents, with rotation reminders | founder | open | |
| Passkey storage and browser use | founder | promoted | 8.5a and 8.5b; accepted KeePassXC coverage under D-0090, with interoperable storage and relying-party behavior to verify. |
| `keypaste env set --no-history` for rotating a leaked value | founder | open | Diverges from KeePassXC's editor, so it must be opt-in and loud (D-0014). |
| `execve` the child on Unix instead of wrapping it | founder | parked | Unix exec would supply exit status, job control and signals directly; Windows still requires a wrapper, so this would maintain two implementations (D-0016). |
| `keypaste run --no-inherit`, the shape `env -i` has | founder | open | Useful for reproducing what CI sees. |
| `keypaste run --skip-unusable` | founder | parked | Only if somebody hits it. A partially injected environment fails somewhere else, later, worse. |
| CI gate reading an exported `.env` with real `dotenv`, `python-dotenv` and `godotenv` | founder | parked | The only way to keep D-0018's portability claim honest, but it puts npm and pip on the three-OS job. Checked by hand against dotenv 17.4.2 for now. |
| `keypaste env export --format json` | founder | parked | Only once something asks. `--dotenv` is required today precisely so a second format can be added without changing what the first means. |
| `keypaste env diff <project> [file]` | founder | open | The natural companion to `pull` and `export`, and it never has to print a value. |
| A `direnv` shim | founder | parked | `keypaste run` scopes exposure to one command; anything direnv-shaped puts values in the interactive shell and everything it launches. Resolve that first. |
| Vault health report: reuse, expiry and weak values | founder | promoted | V.9 performs local checks; P.8 separately verifies an opt-in breach-check design that respects PRODUCT §3.5. |
| Team approval quorum, "2 of 3 for production" | founder | parked | Requires a concrete team-pilot need after the base approval and membership contracts. |
| Windows Hello / Touch ID unlock | founder | promoted | 4.10a/b; quick unlock must preserve full vault unlocking and recovery requirements. |
| Import wizards for 1Password, Bitwarden, LastPass | founder | promoted | 9.1a–9.1f include KeePassXC CSV; migration needs visible loss reporting and preserves the source until verification. |
| Anomaly nudges for unusual production-credential requests | founder | open | |
| Pasting into the master-password field | founder | open | `Ctrl/Cmd+V` does nothing in the three master-password fields: they leave `AllowsPaste` unset, so no clipboard read happens on their behalf. The read path and the refusal rule now exist for the entry and variable fields (D-0225), so enabling it is one property and a decision about whether a master password should arrive that way. |
| An accessible name on the master-password field | founder | open | Its peer reports no name, so a screen-reader user reaches an unnamed field. It must be constant to avoid the bound-name leak checked by D-0099. |
| Public trust page: reproducible builds, audit fund, bounty | founder | open | Ties to O-0012; reproducibility is not claimed today. |
| Break-glass emergency access with mandatory after-the-fact review | founder | parked | Requires a concrete organization recovery design and pilot need; convenience never softens the audit or PRODUCT §3. |
| Browser extension with autofill, save and update | founder | promoted | 8.3 and its browser children belong to the daily-use Working proposition. D-0090 supersedes the former ordering behind the first payment; store publication and native pairing are release work. |
| Browser extension as an approval surface for agents that live in the browser | founder | promoted | 8.1 supplies the native messaging foundation for browser integration and approvals; only approved scopes reach the running agent. |
| A UX bench: HEART minus the two dimensions law 3.5 forbids measuring | founder | promoted | 4.5 makes task success and usability falsifiable before reliance; engagement and retention telemetry remain excluded by law 3.5. |
| Headless render gates so a screenshot is evidence rather than a memory | founder | promoted | 4.6 closes the GUI secret-display gap described in O-0020; native platform installation still needs independent release evidence. |
| Supported phone workflow and mobile delivery decision | founder | promoted | M.1, M.2a–c and M.3; D-0090 requires a supported phone workflow before paid consumer release. Third-party KDBX compatibility is not proof of keypaste account/sync support. |
| Series: "Secrets hygiene for the agent era", one post per THREATS.md entry | founder | open | Marketing. |
| Comparison pages vs .env, vs Infisical, vs plain KeePass | founder | open | Compare dated, evidenced workflows and availability; local use, managed hosting and teams each need accurate scope. |
| A public demo vault anyone can point Claude at | founder | open | Lets people feel the approval flow safely. |
| Conference talk: "I let Claude into my password manager safely" | founder | open | |
| Contribute KDBX compat fixes upstream to KeePassXC | founder | open | Respectfully. Never fork-and-fight. |
| Daemon architecture for approvals vs per-invocation unlock | founder | promoted | D-0054 chooses the running agent as pipe owner and the app as client; 4.3 completes the native approval surface. |
| Hardware-key challenge-response unlock | founder | promoted | P.1; accepted KeePassXC coverage under D-0090, with platform/device verification and recovery limits. |
| `policy.toml` inside the vault, synced and encrypted | founder | open | |
| Product vocabulary: "agents" vs "clients" vs "apps" | founder | open | Needs user testing. |
| Windows vault saves without Transactional NTFS | founder | open | KeePassLib uses deprecated TxF. On 2026-09-04, ten save tests failed in Windows run 33915931104 with "the function attempted to use a name that is reserved for use by another transaction"; the same commit passed the preceding push run and a retry. Replacing TxF was deferred because a local vendor patch changes the upstream comparison maintained under `third_party/Directory.Build.props` (D-0007). |
| A standalone GitHub organisation, and moving the repository into it | founder | rejected | Tried on 2026-09-04 and undone the next day (D-0082). Runners are installed per account, so a fresh organisation is a fresh customer to them and every workflow queued until it was verified; meanwhile eight published URLs had to move and move back. A one-person project gains nothing from it. The organisation is held and empty; revisit only when there is a second maintainer who needs a role. |

## Not in git

Record locations without linking to private material or copying its values. These are process records confirmed by a person; repository checks cannot establish their external state.

### Private notes

| what | where | last confirmed |
|---|---|---|
| Business model, positioning, acquisition path, pivot and failure conditions | `~/Nextcloud/keypaste/business.md` | 2026-09-04 (exists; not read for this audit) |
| Tier ladder with figures, KPI targets, comparable pricing with sources and fetch dates | `~/Nextcloud/keypaste/keypastebusinessnotes-2026-09-04.md`, which is the durable copy; a working copy may also sit as `keypastebusinessnotes.md` in the tree, gitignored by `.gitignore` rule `*businessnotes*.md` (`git check-ignore` confirms it, and `git log --all -- keypastebusinessnotes.md` is empty). The tree copy is machine-local: on 2026-09-04 it was 34 lines ahead of every other copy and would have gone with the machine it sat on, so the synced copy is the one to trust | 2026-09-04 |

D-0006 removed this material from the roadmap and the parking lot, and D-0072 keeps it out of every commit. PRODUCT owns the current commercial shape; the working file holds dated financial assumptions, and STEPS requires owner-approved prices before paid publication. Earlier assumptions do not override PRODUCT v1.2 or its release gates.

---

### Infrastructure

| what | where | note |
|---|---|---|
| keypaste.com landing page | Cloudflare Worker `keypaste-site`; source is `site/` in this repo, deployed by Cloudflare's Git integration on a push to `main` with root directory `site` and no build command; `npx wrangler deploy` from that directory is the local escape hatch | the only server-side code the project runs |
| Release downloads | Cloudflare R2, served at `dl.keypaste.com` | `release.yml` uploads only through `scripts/publish-release.sh`, which refuses a version that already exists and any destination it cannot positively verify as empty, reading a key count rather than an exit status (D-0104, D-0106) |
| Signup database | PlanetScale Postgres, table `public.signup`, reached through Cloudflare Hyperdrive | schema is `site/schema.sql` |
| The database role's password | an account-level Cloudflare Hyperdrive config only; absent from repository, Worker and environment variables | rotating it means `alter role … password` plus `wrangler hyperdrive update`; see `site/README.md` |
| Hyperdrive binding id | a noncredential handle committed in `site/wrangler.jsonc` | |
| Vulnerability reports | `security@keypaste.com`, routed by Cloudflare Email Routing | the only reporting channel that works today |
| DNS for `keypaste.com` and `dl.keypaste.com` | Cloudflare | |
| CI runners | GitHub-hosted runners only: `ubuntu-24.04`, `ubuntu-22.04`, `ubuntu-22.04-arm`, `windows-2025`, `macos-15`; no Blacksmith label remains in any workflow | free for a public repository (D-0086) |
| Sync relay host and its S3-compatible bucket | not yet held; H.7 enrollment (H-0019), H.8 deployment | the relay is one binary (D-0064); the hosted instance is that binary on a small VM behind Cloudflare |
| Stripe account for Individual and Team billing | not yet held; 5.5a enrollment (H-0018) | 5.5b/c implement relay-side entitlements and cancellation; payment never gates local client functionality |
| Microsoft Artifact Signing for Windows binaries (formerly Azure Trusted Signing) | not yet held; 3.6a enrollment (H-0017) | D-0070, D-0143: signed through `signtool /dlib`; an OV certificate in a cloud HSM only if eligibility is refused; the dlib is pinned in release-targets.json (D-0204) |
| Apple Developer Program for notarization | not yet held; H-0015 | D-0057 |

Cloudflare Git integration deploys `site/` on a push to `main`, without a build job or GitHub deployment workflow. `ci.yml` checks the dialog and log examples in `site/public/index.html` through `scripts/verify-demo.sh` at the checked-out ref. Run `verify-site-disclosure.sh` and `verify-site-endpoint.sh` manually against the live origin; database-role, grant and CA checks remain separate under H-0011 in `site/README.md`.

---

### Names not yet held

| what | where | status |
|---|---|---|
| GitHub org `keypaste` | github.com | registered 2026-09-04 and held empty to reserve the name; the repository stays under `notinferred` (D-0082) |
| npm and crates names | npmjs.com, crates.io | not registered or sought; neither is a distribution channel (D-0053, D-0082) |
| Trademark on "keypaste" | — | not filed; D-0058 accepts the risk based on D-0053's one known collision, without a full clearance search. A second collision triggers review |

The repository itself is `notinferred/keypaste` and is public, recreated at the same URL on 2026-09-06 (D-0089). The earlier H-0003 publication precondition is complete; current launch readiness lives in `docs/STEPS.md`.

---

### Recordings and fixtures

| what | where | note |
|---|---|---|
| The demo GIF | `docs/demo/keypaste-demo.gif`, mirrored at `site/public/demo/keypaste-demo.gif` | exists: 63,614 bytes (about 62 KiB); rendered from verified output and a captured Claude Code session, D-0088; step 3.1 is complete |
| KeePassXC for the Windows compat job | fetched from the official zip, pinned by SHA-256 in `ci.yml` | changing the pin is a security decision |
| WiX 5.0.2 for the Windows installer | `WixToolset.Sdk` pinned by version in the wixproj and by SHA-512 in `app.yml`; `WixToolset.Heat` in `Directory.Packages.props` and `packaging/windows/packages.lock.json` | moving past v5 changes the licence terms, D-0139 |
| Artifact Signing dlib 1.0.128 | `Microsoft.ArtifactSigning.Client` downloaded by `packaging/windows/signing/Keypaste.Signing.csproj`, held to its nupkg SHA-512 and x64 dll SHA-256 in `release-targets.json` `signing.dlib` | changing the pin is a security decision, D-0204 |
| Published `v0.1.0` assets | `https://dl.keypaste.com/v0.1.0/`: four native binaries, corresponding source and checksums | immutable; the pipeline will not republish a version |

---

### Upstream

| what | where | note |
|---|---|---|
| KeePass source | vendored from the KeePass 2.61 netstandard port, commit and licence recorded in `third_party/KeePassLib/UPSTREAM.md` | GPL-2.0-or-later; two documented `#if` changes, nothing else |

---

<a id="archive--the-reasoning-long-form"></a>

## Archive

The records frozen on 2026-09-05 (D-0083) are retained at [commit 083fa45](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L229). This index preserves every historical section anchor and its recorded metadata; links open the unchanged reasoning and evidence. The active ledger, STEPS and product pages own current decisions and status.

| Record | Recorded metadata | Source |
|---|---|---|
| <a id="d-0001--project-naming-pascalcase-projects-kebab-case-binaries"></a>D-0001 — Project naming: PascalCase projects, kebab-case binaries |  | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L233) |
| <a id="d-0002--target-framework-net100"></a>D-0002 — Target framework `net10.0` |  | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L241) |
| <a id="d-0003--test-stack-xunit-v3-on-microsofttestingplatform"></a>D-0003 — Test stack: xUnit v3 on Microsoft.Testing.Platform |  | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L247) |
| <a id="d-0004--dependencies-central-versions-plus-committed-lock-files"></a>D-0004 — Dependencies: central versions plus committed lock files |  | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L262) |
| <a id="d-0005--build-gates-encode-the-security-laws"></a>D-0005 — Build gates encode the security laws |  | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L276) |
| <a id="d-0007--kdbx4-via-vendored-keepasslib-not-a-nuget-package"></a>D-0007 — KDBX4 via vendored KeePassLib, not a NuGet package |  | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L290) |
| <a id="d-0008--the-keepassxc-compatibility-gate-is-permanent"></a>D-0008 — The KeePassXC compatibility gate is permanent |  | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L312) |
| <a id="d-0009--the-cli-contract-exit-codes-stream-split-one-line-per-prompt"></a>D-0009 — The CLI contract: exit codes, stream split, one line per prompt |  | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L334) |
| <a id="d-0010--hidden-input-stderr-prompts-utf-8-pipes-and-no-string"></a>D-0010 — Hidden input: stderr prompts, UTF-8 pipes, and no `string` |  | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L352) |
| <a id="d-0011--the-clipboard-is-an-os-tool-over-stdin-auto-clear-blocks-behind-a-seam"></a>D-0011 — The clipboard is an OS tool over stdin; auto-clear blocks, behind a seam |  | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L364) |
| <a id="d-0012--toolskeypastecompatfixture-retired-the-gate-runs-the-shipped-binary"></a>D-0012 — `tools/Keypaste.CompatFixture` retired; the gate runs the shipped binary |  | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L382) |
| <a id="d-0013--file-transactions-apply-on-open-not-only-on-create"></a>D-0013 — File transactions apply on open, not only on create |  | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L394) |
| <a id="d-0014---env-sets-are-one-kdbx-entry-per-variable-not-custom-string-fields"></a>D-0014 - Env sets are one KDBX entry per variable, not custom string fields |  | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L404) |
| <a id="d-0015---env-pull-is-fail-closed-and-it-deletes-the-env-rather-than-claiming-to-shred-it"></a>D-0015 - `env pull` is fail-closed, and it deletes the .env rather than claiming to shred it |  | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L430) |
| <a id="d-0016---keypaste-run-the-argument-split-the-merge-the-signals-the-exit-codes"></a>D-0016 - `keypaste run`: the argument split, the merge, the signals, the exit codes |  | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L470) |
| <a id="d-0017---saving-a-vault-retries-a-transient-file-error-and-says-why-when-it-gives-up"></a>D-0017 - Saving a vault retries a transient file error, and says why when it gives up |  | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L506) |
| <a id="d-0018---env-export-writes-plaintext-and-single-quotes-are-what-make-it-portable"></a>D-0018 - `env export` writes plaintext, and single quotes are what make it portable |  | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L538) |
| <a id="why-this-does-not-break-law-34"></a>Why this does not break law 3.4 |  | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L546) |
| <a id="single-quotes-not-escaped-double-quotes---the-one-real-correctness-decision"></a>Single quotes, not escaped double quotes - the one real correctness decision |  | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L554) |
| <a id="fail-closed-and-the-boundary-the-two-ends-have-to-share"></a>Fail closed, and the boundary the two ends have to share |  | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L584) |
| <a id="the-file-it-leaves-behind"></a>The file it leaves behind |  | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L592) |
| <a id="the-colour-and-why-it-is-not-one-code-path"></a>The colour, and why it is not one code path |  | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L598) |
| <a id="a-reader-bug-found-while-writing-the-writer"></a>A reader bug found while writing the writer |  | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L604) |
| <a id="what-the-tests-had-to-be-to-be-worth-having"></a>What the tests had to be, to be worth having |  | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L608) |
| <a id="d-0019---the-first-dependency-in-src-modelcontextprotocolcore-and-nothing-more"></a>D-0019 - The first dependency in `src/`: ModelContextProtocol.Core, and nothing more |  | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L616) |
| <a id="the-aot-gate-passes-and-the-reason-it-was-expected-not-to-was-wrong"></a>The AOT gate passes, and the reason it was expected not to was wrong |  | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L630) |
| <a id="the-tools-are-still-hand-written-for-the-reason-that-survived"></a>The tools are still hand-written, for the reason that survived |  | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L641) |
| <a id="migration-cost-agreed-in-advance"></a>Migration cost, agreed in advance |  | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L647) |
| <a id="d-0020---the-audit-log-a-precondition-not-observability"></a>D-0020 - The audit log: a precondition, not observability |  | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L651) |
| <a id="no-record-no-disclosure"></a>No record, no disclosure |  | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L665) |
| <a id="two-servers-share-one-file-and-the-obvious-implementation-loses-lines"></a>Two servers share one file, and the obvious implementation loses lines |  | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L671) |
| <a id="what-append-only-claims"></a>What "append-only" claims |  | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L679) |
| <a id="d-0021---exposure-the-listing-surface-is-default-deny-too"></a>D-0021 - Exposure: the listing surface is default-deny too |  | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L683) |
| <a id="d-0022---the-vault-stays-locked-and-the-seam-that-says-so"></a>D-0022 - The vault stays locked, and the seam that says so |  | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L703) |
| <a id="the-cost-stated-rather-than-hidden"></a>The cost, stated rather than hidden |  | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L715) |
| <a id="d-0023---the-approver-is-a-separate-process-the-human-starts"></a>D-0023 - The approver is a separate process the human starts |  | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L724) |
| <a id="what-it-costs-stated-rather-than-buried"></a>What it costs, stated rather than buried |  | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L742) |
| <a id="d-0024---the-approver-channel-is-a-named-pipe-with-currentuseronly"></a>D-0024 - The approver channel is a named pipe with CurrentUserOnly |  | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L754) |
| <a id="d-0025---the-approval-window-is-45-seconds-not-the-60-the-specification-asks-for"></a>D-0025 - The approval window is 45 seconds, not the 60 the specification asks for |  | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L772) |
| <a id="d-0026---the-grant-cache-stores-the-value-keyed-on-the-connection"></a>D-0026 - The grant cache stores the value, keyed on the connection |  | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L788) |
| <a id="the-residual-which-is-real-threatsmd-t-12"></a>The residual, which is real (THREATS.md T-12) |  | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L804) |
| <a id="d-0027---two-ways-of-saying-no-are-not-one-way"></a>D-0027 - Two ways of saying no are not one way |  | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L812) |
| <a id="d-0028---the-policy-file-is-used-whole-or-ignored-whole-and-keypaste-parses-it-itself"></a>D-0028 - The policy file is used whole or ignored whole, and keypaste parses it itself |  | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L832) |
| <a id="all-of-the-file-or-none-of-it"></a>All of the file, or none of it |  | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L838) |
| <a id="the-parser-is-hand-written-and-the-strictness-is-the-feature"></a>The parser is hand-written, and the strictness is the feature |  | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L848) |
| <a id="nothing-defaults-and-no-rule-string-may-lie-about-what-it-says"></a>Nothing defaults, and no rule string may lie about what it says |  | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L858) |
| <a id="the-trap-in-the-pattern-syntax-and-why-the-renderer-exists"></a>The trap in the pattern syntax, and why the renderer exists |  | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L864) |
| <a id="a-spent-allowance-denies-rather-than-escalating"></a>A spent allowance denies rather than escalating |  | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L871) |
| <a id="policy-ls-needs-no-vault-and-there-is-no-policy-add"></a>`policy ls` needs no vault, and there is no `policy add` |  | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L877) |
| <a id="d-0029---where-the-policy-sits-in-the-order-and-what-it-deliberately-does-not-touch"></a>D-0029 - Where the policy sits in the order, and what it deliberately does not touch |  | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L885) |
| <a id="both-ceilings-apply-and-the-arithmetic-is-not-the-callers"></a>Both ceilings apply, and the arithmetic is not the caller's |  | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L906) |
| <a id="a-policy-grant-does-not-seed-the-grant-cache"></a>A policy grant does not seed the grant cache |  | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L912) |
| <a id="the-listing-path-is-not-handed-the-policy-at-all"></a>The listing path is not handed the policy at all |  | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L916) |
| <a id="d-0030---a-rule-keys-on-the-label-the-operator-wrote-not-the-name-the-agent-asserts"></a>D-0030 - A rule keys on the label the operator wrote, not the name the agent asserts |  | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L922) |
| <a id="the-label-travels-raw-and-the-wire-version-does-not-move"></a>The label travels raw, and the wire version does not move |  | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L932) |
| <a id="d-0031---the-audit-chain-commits-to-bytes-and-forgives-the-damage-a-machine-does-to-itself"></a>D-0031 - The audit chain commits to bytes, and forgives the damage a machine does to itself |  | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L938) |
| <a id="the-writer-reads-the-file-so-it-opens-the-file-twice"></a>The writer reads the file, so it opens the file twice |  | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L948) |
| <a id="seq-becomes-the-files-and-stays-advisory"></a>`seq` becomes the file's, and stays advisory |  | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L956) |
| <a id="the-writer-records-around-what-it-did-not-write-and-refuses-over-one-thing"></a>The writer records around what it did not write, and refuses over one thing |  | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L962) |
| <a id="genesis-is-zeros-and-only-one-path-reaches-it"></a>Genesis is zeros, and only one path reaches it |  | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L968) |
| <a id="not-crying-wolf-is-half-the-mitigation"></a>Not crying wolf is half the mitigation |  | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L974) |
| <a id="an-unverifiable-record-is-a-hole-unless-it-is-marked"></a>An unverifiable record is a hole unless it is marked |  | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L982) |
| <a id="what-is-not-claimed"></a>What is not claimed |  | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L989) |
| <a id="d-0032---keypaste-log-sanitizes-on-the-way-out-and-fails-on-a-broken-chain"></a>D-0032 - `keypaste log` sanitizes on the way out, and fails on a broken chain |  | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L995) |
| <a id="d-0033---the-demo-is-a-recorded-real-session-and-the-recording-is-committed-as-text"></a>D-0033 - The demo is a recorded real session, and the recording is committed as text |  | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L1011) |
| <a id="the-geometry-is-a-measurement"></a>The geometry is a measurement |  | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L1025) |
| <a id="not-a-ci-gate-and-it-cannot-become-one"></a>Not a CI gate, and it cannot become one |  | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L1031) |
| <a id="d-0034---the-demo-page-is-held-to-the-binaries-and-claude-is-deliberately-not-in-ci"></a>D-0034 - The demo page is held to the binaries, and Claude is deliberately not in CI |  | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L1035) |
| <a id="two-things-the-harness-cannot-see-and-does-not-pretend-to"></a>Two things the harness cannot see, and does not pretend to |  | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L1045) |
| <a id="d-0035---the-bridge-waits-half-a-second-for-an-approver-not-two"></a>D-0035 - The bridge waits half a second for an approver, not two |  | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L1051) |
| <a id="d-0036---the-launch-page-claims-only-what-a-gate-or-a-citation-can-hold"></a>D-0036 - The launch page claims only what a gate or a citation can hold |  | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L1065) |
| <a id="d-0037---keypastecom-collects-email-addresses-and-cannot-read-them-back"></a>D-0037 - keypaste.com collects email addresses, and cannot read them back |  | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L1081) |
| <a id="what-was-deliberately-not-shipped"></a>What was deliberately not shipped |  | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L1097) |
| <a id="the-commitment-this-record-exists-to-hold"></a>The commitment this record exists to hold |  | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L1103) |
| <a id="what-is-deployed-established-by-probing-it"></a>What is deployed, established by probing it |  | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L1109) |
| <a id="d-0038---the-launch-essay-was-retitled-because-the-title-docsstepsmd-gave-it-was-false"></a>D-0038 - The launch essay was retitled because the title docs/STEPS.md gave it was false |  | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L1121) |
| <a id="d-0039---the-launch-copy-is-written-and-the-launch-is-blocked-by-a-list-of-things-that-are-false"></a>D-0039 - The launch copy is written, and the launch is blocked by a list of things that are false |  | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L1139) |
| <a id="d-0041---a-release-is-four-native-builds-that-are-run-before-they-are-uploaded"></a>D-0041 - A release is four native builds that are run before they are uploaded |  | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L1159) |
| <a id="d-0042---ci-runs-on-one-provider-now-because-billing-stopped-the-other-one"></a>D-0042 - CI runs on one provider now, because billing stopped the other one |  | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L1185) |
| <a id="d-0043---the-install-gate-had-never-run-and-neither-reason-was-laziness"></a>D-0043 - The install gate had never run, and neither reason was laziness |  | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L1203) |
| <a id="d-0044---the-desktop-shell-is-avalonia-and-neither-of-the-two-shells-this-repository-had-already-named-survived-being-checked"></a>D-0044 - The desktop shell is Avalonia, and neither of the two shells this repository had already named survived being checked |  | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L1217) |
| <a id="d-0045---what-a-screen-may-show-decided-because-a-test-made-somebody-decide"></a>D-0045 - What a screen may show, decided because a test made somebody decide |  | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L1253) |
| <a id="d-0046---the-clipboard-rule-is-shared-the-transport-is-not"></a>D-0046 - The clipboard rule is shared, the transport is not |  | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L1269) |
| <a id="d-0047---the-masked-value-cell-answers-a-threat-t-22-only-half-described"></a>D-0047 - The masked value cell answers a threat T-22 only half described |  | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L1285) |
| <a id="d-0048---a-save-that-would-revert-somebody-elses-write-is-refused"></a>D-0048 - A save that would revert somebody else's write is refused |  | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L1297) |
| <a id="d-0049---the-two-front-ends-get-one-test-project-and-it-is-in-neither-solution"></a>D-0049 - The two front ends get one test project, and it is in neither solution |  | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L1313) |
| <a id="d-0050---the-keepassxc-gate-covers-the-apps-writes-and-that-is-checked-rather-than-asserted"></a>D-0050 - The KeePassXC gate covers the app's writes, and that is checked rather than asserted |  | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L1325) |
| <a id="d-0051---the-documentation-is-a-system-with-a-termination-condition-not-a-pile"></a>D-0051 - The documentation is a system with a termination condition, not a pile |  | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L1339) |
| <a id="open-decisions"></a>Open decisions | Historical questions and subsequent answers | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L1357) |
| <a id="o-0015---bundling-and-signing-a-desktop-app-when-the-official-bundler-is-a-paid-product"></a>O-0015 - Bundling and signing a desktop app, when the official bundler is a paid product |  | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L1359) |
| <a id="o-0016---the-desktop-apps-size-which-is-not-the-clis"></a>O-0016 - The desktop app's size, which is not the CLI's |  | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L1367) |
| <a id="o-0017---who-owns-the-approver-pipe-once-the-app-can-approve"></a>O-0017 - Who owns the approver pipe once the app can approve | Answered by D-0054: the agent owns the pipe and the app is a client of it | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L1377) |
| <a id="o-0002--contribution-terms-dco-or-cla"></a>O-0002 — Contribution terms: DCO or CLA | Answered by D-0055: DCO | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L1383) |
| <a id="d-0006--business-notes-live-outside-the-repository-and-publishing-is-irreversible"></a>D-0006 — Business notes live outside the repository, and publishing is irreversible |  | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L1389) |
| <a id="o-0004--deferred-ci-hardening"></a>O-0004 — Deferred CI hardening | Partly answered in 3.4; CodeQL and dependency-review deferred | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L1399) |
| <a id="o-0005--arm64-and-the-native-toolchain"></a>O-0005 — arm64 and the native toolchain | Answered in 3.4 | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L1405) |
| <a id="d-0040---vendored-keepasslib-survives-nativeaot-proved-by-running-the-published-binaries"></a>D-0040 - Vendored KeePassLib survives NativeAOT, proved by running the published binaries |  | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L1409) |
| <a id="o-0007--trim-the-vendored-tree"></a>O-0007 — Trim the vendored tree? | Open; AOT did not require trimming | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L1435) |
| <a id="o-0008--windows-clipboard-history-and-cloud-sync-retain-the-secret"></a>O-0008 — Windows clipboard history and cloud sync retain the secret | Closed on both front ends by construction; the end-to-end check is still BLOCKED | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L1443) |
| <a id="o-0009---values-on-the-command-line-and-case-colliding-names-on-windows"></a>O-0009 - Values on the command line, and case-colliding names on Windows | Only the `argv` half of this entry is still open | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L1457) |
| <a id="o-0010---the-released-binaries-are-unsigned-and-un-notarized"></a>O-0010 - The released binaries are unsigned and un-notarized | Answered by D-0057, and split: macOS is notarized, Windows is not | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L1469) |
| <a id="o-0011---there-is-no-homebrew-tap-no-scoop-bucket-and-no-winget-manifest"></a>O-0011 - There is no Homebrew tap, no Scoop bucket and no winget manifest |  | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L1477) |
| <a id="o-0012---the-builds-are-not-reproducible-and-nothing-attests-to-where-they-came-from"></a>O-0012 - The builds are not reproducible, and nothing attests to where they came from |  | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L1485) |
| <a id="o-0013---osx-x64-win-arm64-and-linux-musl-x64-are-unserved"></a>O-0013 - osx-x64, win-arm64 and linux-musl-x64 are unserved |  | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L1493) |
| <a id="o-0014---the-repository-is-private-and-docsproductmd-says-the-code-is-open"></a>O-0014 - The repository is private and docs/PRODUCT.md says the code is open | Answered by H-0003 on 2026-08-04: public once a release actually works, and not before the launch posts | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L1505) |
| <a id="o-0018---two-writers-one-kdbx-and-no-merge"></a>O-0018 - Two writers, one KDBX, and no merge |  | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L1517) |
| <a id="o-0019---macos-could-ask-its-pasteboard-to-conceal-a-secret-and-does-not-yet"></a>O-0019 - macOS could ask its pasteboard to conceal a secret, and does not yet |  | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L1527) |
| <a id="o-0020---nothing-automated-has-ever-seen-this-app-draw"></a>O-0020 - Nothing automated has ever seen this app draw |  | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L1535) |
| <a id="d-0053--cratesio-is-somebody-elses-and-keypaste-does-not-need-it"></a>D-0053 — crates.io is somebody else's, and keypaste does not need it |  | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L1543) |
| <a id="o-0021---an-imported-bundle-is-untrusted-input-and-env-is-what-agents-can-already-see"></a>O-0021 - An imported bundle is untrusted input, and `env/` is what agents can already see |  | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L1555) |
| <a id="o-0022---the-hosted-tier-is-now-an-intention-and-2-permits-exactly-one-shape-of-it"></a>O-0022 - The hosted tier is now an intention, and §2 permits exactly one shape of it | Answered by D-0060: 5.2 as written, and the server cannot read the blob | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L1567) |
| <a id="d-0054--the-agent-owns-the-approver-pipe-the-app-is-a-client-of-it"></a>D-0054 — The agent owns the approver pipe; the app is a client of it |  | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L1581) |
| <a id="d-0055--contribution-terms-dco"></a>D-0055 — Contribution terms: DCO |  | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L1593) |
| <a id="d-0056--the-cli-opts-out-of-windows-clipboard-history-the-argv-exposure-stays-documented"></a>D-0056 — The CLI opts out of Windows clipboard history; the `argv` exposure stays documented |  | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L1603) |
| <a id="d-0057--macos-binaries-are-notarized-windows-binaries-are-unsigned-and-say-so"></a>D-0057 — macOS binaries are notarized; Windows binaries are unsigned and say so |  | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L1615) |
| <a id="d-0058--the-name-is-not-defended-the-risk-is-accepted"></a>D-0058 — The name is not defended; the risk is accepted |  | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L1627) |
| <a id="d-0059--autofill-was-deferred-behind-a-condition-the-re-ratification-overtook-it"></a>D-0059 — Autofill was deferred behind a condition; the re-ratification overtook it |  | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L1637) |
| <a id="d-0060--the-hosted-tier-is-52-as-written-the-server-cannot-read-the-blob"></a>D-0060 — The hosted tier is 5.2 as written: the server cannot read the blob |  | [Record](https://github.com/notinferred/keypaste/blob/083fa45ad50316fa41a731de44b8e84e780d7318/DECISIONS.md#L1643) |
