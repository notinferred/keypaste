{
  "name": "keypaste-site",
  "private": true,
  "type": "module",
  "description": "keypaste.com - static pages, one form endpoint and share links",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test"
  },
  "dependencies": {
    "postgres": "3.4.9"
  },
  "devDependencies": {
    "wrangler": "4.114.0"
  }
}
