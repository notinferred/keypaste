<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <RootNamespace>Keypaste.VaultRestorer</RootNamespace>

    <!-- Not a test project: it holds no tests. It lives under tests/ because nothing shipped
         references it, and it exists only so the history gate has something that can perform a
         restore - V.2a adds no CLI or desktop surface that can. -->
    <IsTestProject>false</IsTestProject>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="../../src/Keypaste.Core/Keypaste.Core.csproj" />
  </ItemGroup>

</Project>
